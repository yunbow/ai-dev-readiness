import { CheckCircle2, CircleAlert, ListChecks, ShieldAlert, Workflow } from "lucide-react";
import { useTranslation } from "react-i18next";
import type { DiagnosisResult, SuitabilityLevel } from "@/domain/assessment/types";
import { Badge } from "@/components/ui/badge";

const processGroupsMeta: Array<{ level: SuitabilityLevel; titleKey: string; descriptionKey: string; icon: typeof CheckCircle2; className: string }> = [
  { level: "recommended", titleKey: "processGroupRecommendedTitle", descriptionKey: "processGroupRecommendedDescription", icon: CheckCircle2, className: "border-emerald-200 bg-emerald-50/50" },
  { level: "conditional", titleKey: "processGroupConditionalTitle", descriptionKey: "processGroupConditionalDescription", icon: CircleAlert, className: "border-amber-200 bg-amber-50/50" },
  { level: "not_recommended", titleKey: "processGroupNotRecommendedTitle", descriptionKey: "processGroupNotRecommendedDescription", icon: ShieldAlert, className: "border-rose-200 bg-rose-50/50" },
];

export function Issues({ result, count }: { result: DiagnosisResult; count: number }) {
  const { t } = useTranslation();
  return <section><div className="flex items-end justify-between gap-4"><div className="flex items-center gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-blue-50 text-blue-700"><ListChecks className="size-5" /></span><div><p className="text-xs font-semibold tracking-[0.16em] text-blue-700">{t("resultSections.priorityActionsEyebrow")}</p><h2 className="mt-1 text-2xl font-semibold tracking-tight text-slate-950">{t("resultSections.priorityActionsTitle")}</h2></div></div><span className="text-sm text-muted-foreground">{t("resultSections.priorityActionsCount", { count: Math.min(result.issues.length, count) })}</span></div>{result.issues.length === 0 && <p className="mt-5 rounded-2xl bg-emerald-50 p-5 text-sm text-emerald-900">{t("resultSections.priorityActionsEmpty")}</p>}<div className="mt-5 grid gap-4">{result.issues.slice(0, count).map((issue, index) => <article key={issue.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div className="flex gap-4"><span className="flex size-9 shrink-0 items-center justify-center rounded-xl bg-blue-50 text-sm font-semibold text-blue-700">0{index + 1}</span><div><div className="flex items-center gap-2"><Badge className="bg-slate-900">{t("resultSections.priorityBadge", { priority: t(`resultSections.priority.${issue.priority}`) })}</Badge><span className="text-xs text-muted-foreground">{issue.category}</span></div><h3 className="mt-3 font-semibold text-slate-950">{t(`recommendations.${issue.id}.title`, issue.title)}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{t(`recommendations.${issue.id}.description`, issue.description)}</p></div></div></div><div className="mt-5 grid gap-3 border-t border-slate-100 pt-4 sm:grid-cols-2"><div><p className="text-xs font-semibold text-blue-700">{t("resultSections.actionLabel")}</p><p className="mt-1 text-sm leading-6 text-slate-700">{t(`recommendations.${issue.id}.action`, issue.action)}</p></div><div><p className="text-xs font-semibold text-blue-700">{t("resultSections.expectedEffectLabel")}</p><p className="mt-1 text-sm leading-6 text-slate-700">{t(`recommendations.${issue.id}.expectedEffect`, issue.expectedEffect)}</p></div></div></article>)}</div></section>;
}

export function StandardSections({ result }: { result: DiagnosisResult }) {
  const { t } = useTranslation();
  return <section><div><div className="flex items-center gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-blue-50 text-blue-700"><Workflow className="size-5" /></span><div><p className="text-xs font-semibold tracking-[0.16em] text-blue-700">{t("resultSections.processFitEyebrow")}</p><h2 className="mt-1 text-2xl font-semibold tracking-tight text-slate-950">{t("resultSections.processFitTitle")}</h2></div></div><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">{t("resultSections.processFitSubtitle")}</p></div><div className="mt-6 grid gap-4 lg:grid-cols-3">{processGroupsMeta.map((group) => { const items = result.processSuitability.filter((item) => item.level === group.level); const Icon = group.icon; return <article key={group.level} className={`rounded-2xl border p-5 ${group.className}`}><div className="flex items-center justify-between gap-3"><span className="flex size-9 items-center justify-center rounded-xl bg-white/80 text-slate-800"><Icon className="size-5" /></span><Badge variant="secondary">{items.length ? t("resultSections.processCount", { count: items.length }) : t("resultSections.processCountNone")}</Badge></div><h3 className="mt-4 font-semibold text-slate-950">{t(`resultSections.${group.titleKey}`)}</h3><p className="mt-1 text-sm leading-6 text-muted-foreground">{t(`resultSections.${group.descriptionKey}`)}</p>{items.length ? <ul className="mt-5 space-y-3">{items.map((item) => <li key={item.process} className="rounded-xl bg-white/80 p-4"><p className="font-medium text-slate-950">{t(`processNames.${item.process}.name`)}</p><p className="mt-1 text-xs leading-5 text-slate-600">{t("resultSections.firstUseCase", { example: t(`processNames.${item.process}.example`) })}</p>{item.noteKey && <p className="mt-2 border-t border-slate-200 pt-2 text-xs leading-5 text-slate-600">{t("resultSections.judgmentPoint", { note: t(`resultSections.${item.noteKey}`) })}</p>}</li>)}</ul> : <div className="mt-5 rounded-xl border border-white/70 bg-white/60 p-4 text-sm leading-6 text-slate-600">{t("resultSections.processEmptyBody")}</div>}</article>; })}</div></section>;
}
